# ============================================================
# Node 2: Broker 2 (KRaft Controller)
# Requirements: 8+ vCPU, 32+ GB RAM, 100+ GB NVMe-equivalent storage
# ============================================================
# IMPORTANT: Must specify services explicitly to avoid starting
# unwanted services from base compose (e.g., Connect, monitoring).
#
# CORRECT USAGE — Specify services explicitly:
#   docker compose -f docker-compose.yml -f docker-compose.broker2.yml \
#     up -d broker node-exporter cadvisor
#
# WRONG (will fail or start unintended services):
#   docker compose -f docker-compose.yml -f docker-compose.broker2.yml up -d
#
# Or use helper script:
#   ./scripts/5-start-node.sh broker2

services:
  broker:
    environment:
      KAFKA_NODE_ID: 2
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://${BROKER_2_IP}:9092

  node-exporter: {}

  cadvisor: {}
